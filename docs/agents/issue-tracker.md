# Issue Tracker

Issues are tracked using GitHub Issues via the `gh` CLI.

## Workflow

- Create issues: `gh issue create --title "<title>" --body "<body>" --label "<label>"`
- View issues: `gh issue list`
- Read issue details: `gh issue view <number>`
