# Domain Documentation Layout

The repository uses a single-context layout:

- `CONTEXT.md`: Ubiquitous domain glossary at the repo root.
- `docs/adr/`: Architecture Decision Records (`0001-slug.md`, etc.).
